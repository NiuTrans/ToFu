"""Paper-image namespace.

``figures`` owns manifests, ``injection`` owns deterministic Markdown placement,
and ``titles`` owns title recovery.  Import the concrete owner.
"""

__all__: tuple[str, ...] = ()
