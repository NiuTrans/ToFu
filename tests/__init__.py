"""ChatUI Test Suite

Markers:
  @pytest.mark.unit   — Pure unit tests (fast, no I/O)
  @pytest.mark.api    — Flask test-client API integration tests
  @pytest.mark.visual — Playwright visual E2E tests (needs browser + mock server)
"""
